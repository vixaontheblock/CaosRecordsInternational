const fs=require('node:fs');const path=require('node:path');const os=require('node:os');const {execFileSync}=require('node:child_process');const ts=require('typescript');
function load(file){const out=ts.transpileModule(fs.readFileSync(file,'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020}}).outputText;const module={exports:{}};new Function('exports','module',out)(module.exports,module);return module.exports;}
const {artists}=load('data/artists.ts');const {officialEmail,phone}=load('lib/site.ts');const esc=x=>String(x||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const base=process.env.NEXT_PUBLIC_SITE_URL||'https://caosrecords.vercel.app';
const logo=fs.readFileSync('public/logo-black.png').toString('base64');fs.mkdirSync('public/dossiers',{recursive:true});
for(const a of artists){const image=path.join('public',a.image);const mime=/\.png$/i.test(image)?'image/png':'image/jpeg';const photo=fs.readFileSync(image).toString('base64');
const html=`<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex"><title>${esc(a.name)} — Dossier CAOS Records</title><style>*{box-sizing:border-box}body{max-width:950px;margin:auto;padding:35px;font:16px/1.7 Arial,sans-serif;color:#111;background:#fff}header{display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #bbb;padding-bottom:18px}header img{width:100px}h1{font-size:clamp(40px,8vw,76px);line-height:1;margin:25px 0}h2{font-size:25px;margin-top:35px}a{color:inherit;overflow-wrap:anywhere}small{letter-spacing:2px}section.cover{display:grid;grid-template-columns:1fr 1fr;gap:40px;margin:35px 0}img.portrait{width:100%;height:470px;object-fit:cover;object-position:${esc(a.imagePosition)};filter:grayscale(1)}li{margin-bottom:12px}footer{border-top:1px solid #aaa;margin-top:35px;padding-top:20px;font-size:12px}.actions a,.actions button{display:inline-block;margin:10px 15px 0 0;padding:12px;background:#111;color:white;border:0;font:12px Arial;text-decoration:none;cursor:pointer}@media(max-width:620px){body{padding:22px}section.cover{grid-template-columns:1fr}img.portrait{height:430px}}@media print{@page{size:A4;margin:16mm}body{padding:0;font-size:11pt}.actions{display:none}h1{font-size:44pt}section.cover{gap:22px}img.portrait{height:320px}h2{break-after:avoid}li,footer{break-inside:avoid}a{text-decoration:underline}}</style></head><body><header><img src="data:image/png;base64,${logo}" alt="CAOS Records"><small>DOSSIER DEL ARTISTA</small></header><section class="cover"><div><h1>${esc(a.name)}</h1><p>${esc(a.genre)}</p><p>${esc(a.bio)}</p></div><img class="portrait" src="data:${mime};base64,${photo}" alt="Retrato de ${esc(a.name)}"></section><h2>Música</h2><ul>${(a.releases||[]).map(r=>`<li><a href="https://www.youtube.com/watch?v=${r.videoId}">${esc(r.title)}</a> — ${esc(r.credit)} · ${esc(r.duration)}</li>`).join('')}</ul><h2>Canales oficiales</h2><ul>${Object.entries(a.links||{}).map(([label,url])=>`<li><a href="${esc(url)}">${esc(label==='appleMusic'?'Apple Music':label==='youtube'?'YouTube':label==='spotify'?'Spotify':'Instagram')}</a></li>`).join('')}</ul><h2>Actuaciones y colaboraciones</h2><p>Contacto de CAOS Records: <a href="mailto:${officialEmail}">${officialEmail}</a><br><a href="${phone.call}">${phone.display}</a> · <a href="${phone.whatsapp}">WhatsApp</a></p><p>Para una propuesta, comparte fecha, ciudad, recinto, formato y presupuesto. La disponibilidad y las condiciones se confirman con el equipo para cada evento.</p><div class="actions"><button onclick="window.print()">IMPRIMIR / GUARDAR COMO PDF</button><a href="${base}/artists/${a.slug}">VER FICHA EN LA WEB</a></div><footer>CAOS Records · Material informativo para prensa y promotores. Consulta con el equipo los usos y créditos de las fotografías. Este dossier no confirma contrataciones.</footer></body></html>`;
fs.writeFileSync(`public/dossiers/${a.slug}.html`,html);const dir=fs.mkdtempSync(path.join(os.tmpdir(),'caos-dossier-'));fs.writeFileSync(path.join(dir,'dossier.html'),html);fs.copyFileSync(image,path.join(dir,`${a.slug}${path.extname(image)}`));fs.copyFileSync('public/logo-black.png',path.join(dir,'caos-logo.png'));fs.writeFileSync(path.join(dir,'LEEME.txt'),'Abre dossier.html en un navegador. Puedes imprimirlo o guardarlo como PDF. La fotografía original y el logo se incluyen por separado. Contacta con CAOS Records para usos y créditos.\n');const dest=path.resolve(`public/dossiers/${a.slug}.zip`);if(fs.existsSync(dest))fs.unlinkSync(dest);execFileSync('zip',['-q','-j',dest,...fs.readdirSync(dir).map(f=>path.join(dir,f))]);fs.rmSync(dir,{recursive:true});console.log('Dossier generado:',a.name);}
